import type { ArchiveGame } from "../../types/archive";
const labels = { win: "Won", loss: "Lost", draw: "Draw" };
export function GameCard({ game }: {game: ArchiveGame}) { return <article className="rounded-vault border border-vault-line bg-vault-surface p-7 transition duration-300 hover:-translate-y-1 hover:bg-[#f1eadc]"><div className="flex justify-between text-xs font-bold uppercase tracking-[.13em]"><span className="border border-vault-line bg-vault-canvas px-3 py-2 normal-case tracking-normal">{labels[game.result]}</span><time>{game.createdAt as string}</time></div><h2 className="mt-8 text-2xl font-bold">{game.opening}</h2><p className="mt-2 text-lg text-vault-secondary">{game.variation}</p><div className="my-6 border-t border-vault-line"/><div className="grid grid-cols-[1fr_auto_1fr] items-center"><div><p className="text-xs font-bold uppercase tracking-[.14em]">White</p><b>User</b><p>({game.playerRating})</p></div><strong className="font-display text-5xl text-[#d7c8c1]">{game.result === "draw" ? "½-½" : game.result === "win" ? "1-0" : "0-1"}</strong><div className="text-right"><p className="text-xs font-bold uppercase tracking-[.14em]">Black</p><b>{game.opponent}</b><p>({game.opponentRating})</p></div></div></article>; }
