const stats = [{ value: "1,240", label: "Games" }, { value: "12", label: "Collections" }, { value: "48", label: "Favorites" }];
export function StatSummary() { return <section className="grid grid-cols-3 overflow-hidden rounded-vault border border-vault-line bg-vault-surface animate-rise-in" aria-label="Archive summary">{stats.map((stat, index) => <div key={stat.label} className={`px-3 py-6 text-center sm:px-7 ${index ? "border-l border-vault-line" : ""}`}><p className="font-display text-3xl font-bold text-vault-goldDark sm:text-5xl">{stat.value}</p><p className="mt-2 text-[10px] font-bold uppercase tracking-[.17em]">{stat.label}</p></div>)}</section>; }
