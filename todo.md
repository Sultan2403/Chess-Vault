# Tasks

- Finalize and decide on product pricing and pro features and such
- Backend is done ig... I can't find any more excuses not to work on the client side lol so...

### GET TO WORK DUDE!!!

#### GIVE US A UI!!!!!!!!

## Frontend follow-ups

- [ ] Add `id: z.string()` to the canonical shared `GameSchema` so its inferred `Game` type matches the backend's serialized JSON game object.
- [ ] Remove the frontend-only `ArchiveGame` fixture model and render game UI from the shared `Game` type. Derive player/opponent perspective by matching each game's platform and player usernames against the current user's linked account usernames.
- [ ] Replace temporary static game fixtures with API-hook data once the frontend API layer is initialized.
