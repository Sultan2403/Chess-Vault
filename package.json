{
  "name": "chess-vault",
  "private": true,
  "workspaces": [
    "Backend",
    "Frontend",
    "Shared"
  ],
  "scripts": {
    "dev": "concurrently \"bun run dev:backend\" \"bun run dev:frontend\"",
    "dev:backend": "bun --cwd Backend dev",
    "dev:frontend": "bun --cwd Frontend dev",
    "test:backend": "bun --cwd Backend test",
    "build:shared": "npm run build --workspace=@chess-vault/shared",
    "build:backend": "npm run build --workspace=chess-vault-backend",
    "build:frontend": "npm run build --workspace=chess-vault-frontend",
    "deploy:backend": "npm run build:shared && npm run build:backend",
    "deploy:frontend": "npm run build:shared && npm run build:frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}