const import_Chess_Com_Game = async ({
  userId,
  folderId,
}: {
  userId: string;
  folderId: string;
}) => {
  
};

const import_Lichess_Game = async ({
  userId,
  folderId,
}: {
  userId: string;
  folderId: string;
}) => {};
